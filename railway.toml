[build]
builder = "NIXPACKS"

[deploy]
startCommand = "alembic upgrade head; uvicorn api.main:app --host 0.0.0.0 --port $PORT & python -m bot.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
